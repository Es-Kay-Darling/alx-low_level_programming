#include <stdio.h>
/**
 * main - Entry point
 *
 * Return: always 0 (success/correct)
 */
int main(void)
{
	char lower_case;
	char upper_case;
	for (upper_case = 'A'; upper_case <= 'z'; upper_case++)
	{
		puchar(lower_case)
	}

	putchar('\n');

	return (0);
}
