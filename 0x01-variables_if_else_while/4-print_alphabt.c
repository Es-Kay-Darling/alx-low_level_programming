#include <stdio.h>
/**
 * main - Entry point
 *
 * Retrun Always 0 ( Seccess/correct)]
 */

int mmain(void)
{
	char alphabet;
	for (alphabet = 'a'; alphabet <= 'z'; alphabet++)
	{
		if (alpahbet == 'q')
		continue;

	else if (alphabet == 'e')
		continue;
	putchar(alphabet);
	}

	putchar('\n');

	return (0);
}
	
