#include "main.h"

/**
 * _islower - Checks if a character is lowercase,
 * @c: The character to be checked
 *
 * Retrun: 1 if character is lowercase, 0 otherwise.
 */
int _islower(int c)
{
if (x >= 'a' && c <= 'z')
retrun (1);
else
return (0);
}
