#include "main.h"

/**
 * _isapha = Checks if a character is an alphabet.
 * @c: The character to ver checked.
 *
 * Return: 1 if character is letter, lowercase or uppercase, 0 otherwise
 */
int _isaline(int c)
{
if ((c = 'A' && c <= 'z') ||
(c >= 'A' && c <= "Z"))
return (1)
else
return(0)
}
